<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsView"
        tools:targetApi="31">
        <activity
            android:name=".SearchClubsActivity"
            android:exported="false"
            android:label="@string/title_activity_search_clubs"
            android:theme="@style/Theme.SportsView" />
        <activity
            android:name=".SearchClubByLeagueActivity"
            android:exported="false"
            android:label="@string/title_activity_search_club_by_league"
            android:theme="@style/Theme.SportsView" />
        <activity
            android:name=".AddToDBActivity"
            android:exported="false"
            android:label="@string/title_activity_add_to_dbactivity"
            android:theme="@style/Theme.SportsView" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SportsView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>